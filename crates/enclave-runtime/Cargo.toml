[package]
name = "coprocessor-enclave-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "coprocessor_enclave_runtime"
path = "src/lib.rs"

[dependencies]
coprocessor-ciphertext-binding = { path = "../ciphertext-binding" }
coprocessor-handle-graph-core = { path = "../handle-graph-core" }
